/*import React from "react";
import UpdateGames from "../AccountSettings/UpdateGame";

const mapImages = () => {
  const test = [Game1, Game2, Game3];
  console.log(test);

  const gameImages = [
    { id: 1, title: Valorant },
    { id: 2, title: RocketLeague },
    { id: 3, title: CSGO },
  ];

  const images = gameImages.map((image) => {
    return (
      <img
        key={image}
        src={require(`../backend/logo_images/${title}.png`)}
        className="img-responsive"
      />
    );
  });
};
export default mapImages;
*/
